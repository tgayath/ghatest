name: Trigger auto deployment for contmewd

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '**'
    - '.github/workflows/contmewd-AutoDeployTrigger-1487bb0f-3cbc-4cb9-9446-5e9ab53d8c3f.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.CONTMEWD_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTMEWD_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTMEWD_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: acaregistry.azurecr.io
          registryUsername: ${{ secrets.CONTMEWD_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTMEWD_REGISTRY_PASSWORD }}
          containerAppName: contmewd
          resourceGroup: gayathrirg
          imageToBuild: acaregistry.azurecr.io/contmewd:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


